# ===========================================
# AqStream Environment Variables
# ===========================================
# Скопируйте этот файл в .env и заполните значения
# cp .env.example .env

# === PostgreSQL ===
POSTGRES_USER=aqstream
POSTGRES_PASSWORD=aqstream

# === Redis ===
REDIS_HOST=localhost
REDIS_PORT=6379
# ВАЖНО: Измените пароль для production!
REDIS_PASSWORD=aqstream-redis-secret

# === RabbitMQ ===
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# === MinIO (S3-compatible storage) ===
MINIO_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=aqstream-media

# === JWT ===
# ВАЖНО: Сгенерируйте надёжный ключ для production!
# openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# === Telegram Bot ===
# Получите токен от @BotFather: https://t.me/BotFather
# Инструкция: docs/operations/environments.md#telegram-bot-setup
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=aqstream_bot
# Webhook URL для production (требует HTTPS)
# Если пусто — используется long polling (подходит для локальной разработки)
TELEGRAM_WEBHOOK_URL=https://api.aqstream.ru/notification/telegram/webhook

# === Services (optional overrides) ===
# GATEWAY_PORT=8080
# USER_SERVICE_PORT=8081
# EVENT_SERVICE_PORT=8082
# PAYMENT_SERVICE_PORT=8083
# NOTIFICATION_SERVICE_PORT=8084
# MEDIA_SERVICE_PORT=8085
# ANALYTICS_SERVICE_PORT=8086
