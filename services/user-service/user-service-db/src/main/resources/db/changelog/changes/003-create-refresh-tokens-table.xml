<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!-- Таблица refresh токенов -->
    <changeSet id="003-create-refresh-tokens-table" author="aqstream">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists schemaName="user_service" tableName="refresh_tokens"/>
            </not>
        </preConditions>

        <createTable schemaName="user_service" tableName="refresh_tokens"
                     remarks="Refresh токены для JWT аутентификации. One-time use с rotation.">
            <column name="id" type="UUID" defaultValueComputed="uuid_generate_v4()">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_refresh_tokens_user"
                             references="user_service.users(id)"
                             deleteCascade="true"/>
            </column>

            <!-- Хеш токена (не сам токен) для безопасности -->
            <column name="token_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="expires_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <!-- Флаг отзыва токена -->
            <column name="revoked" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="revoked_at" type="TIMESTAMP WITH TIME ZONE"
                    remarks="Когда токен был отозван"/>

            <!-- Метаданные для аудита -->
            <column name="user_agent" type="VARCHAR(500)"
                    remarks="User-Agent при создании токена"/>
            <column name="ip_address" type="VARCHAR(45)"
                    remarks="IP адрес при создании токена"/>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Индекс для поиска по токену -->
        <createIndex schemaName="user_service" tableName="refresh_tokens"
                     indexName="idx_refresh_tokens_token_hash" unique="true">
            <column name="token_hash"/>
        </createIndex>

        <!-- Индекс для поиска токенов пользователя -->
        <createIndex schemaName="user_service" tableName="refresh_tokens"
                     indexName="idx_refresh_tokens_user_id">
            <column name="user_id"/>
        </createIndex>

        <!-- Индекс для очистки истёкших токенов -->
        <createIndex schemaName="user_service" tableName="refresh_tokens"
                     indexName="idx_refresh_tokens_expires_at">
            <column name="expires_at"/>
        </createIndex>

        <rollback>
            <dropTable schemaName="user_service" tableName="refresh_tokens"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
